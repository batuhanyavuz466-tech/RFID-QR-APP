# Bagimliliklar (node_modules npm install ile yeniden olusur - repoya konmaz)
# package-lock.json BILEREK repoya eklenir (reprodüktif/guvenli deploy icin)
node_modules/

# Ortam/sifre dosyalari
.env

# Uretilen ciktilar (QR'lar panelden/script ile yeniden uretilir)
sunucu/cikti-qr/

# Otomatik veri yedekleri (kalici disk uzerinde tutulur, repoya konmaz)
sunucu/yedekler/
